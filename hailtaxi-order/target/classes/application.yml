server:
  port: 8004
spring:
  #  application:
  #    name: hailtaxi-order
  main:
    allow-bean-definition-overriding: true
  cloud:
    loadbalancer:
      enabled: true
feign:
  sentinel:
    enabled: true
